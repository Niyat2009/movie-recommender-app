# Movie Recommender (Dark Mode)

Run locally:

```
pip install -r requirements.txt
streamlit run app.py
```

Deploy on Streamlit Cloud by uploading these files to a GitHub repo and creating a new Streamlit app.
